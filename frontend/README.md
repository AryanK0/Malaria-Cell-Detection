# Malaria Cell AI Frontend

React + Tailwind CSS interface for the malaria cell detection project.

## Run
```powershell
npm install
npm run dev
```

The app opens at `http://localhost:5173` and calls the FastAPI backend at `http://localhost:5000`.
